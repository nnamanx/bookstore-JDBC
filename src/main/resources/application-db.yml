spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 12131
    url: jdbc:postgresql://localhost:5434/bookstore_jdbc
  config:
    activate:
      on-profile: prod

---

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/bookstore-jdbc-test
    username: postgres
    password: 12131
  config:
    activate:
      on-profile: test